machine:
  services:
    - docker

  post:
    - bash ./install_go.sh

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - make dep

test:
  override:
    - make test

deployment:
  prod:
    branch: master
    commands:
      - make push-container