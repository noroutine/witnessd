machine:
  services:
    - docker
  go:
    version: 1.6
    
test:
  post:
    - make container

dependencies:
  pre:
    - go get github.com/tools/godep
  override:
    - godep restore

deployment:
  prod:
    branch: master
    commands:
      - make push-container

